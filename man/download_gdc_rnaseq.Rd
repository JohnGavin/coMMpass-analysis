% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_data_acquisition.R
\name{download_gdc_rnaseq}
\alias{download_gdc_rnaseq}
\title{Download RNA-seq data from GDC}
\usage{
download_gdc_rnaseq(
  project_id = "MMRF-COMMPASS",
  data_dir = "data/raw/gdc",
  sample_limit = 200,
  random_sample = TRUE,
  seed = 42,
  use_parquet = TRUE
)
}
\arguments{
\item{project_id}{Project identifier (default: "MMRF-COMMPASS")}

\item{data_dir}{Directory to save data}

\item{sample_limit}{Maximum number of samples (NULL for all, default 200)}

\item{random_sample}{If TRUE and sample_limit is set, randomly sample
patients using seed for reproducibility (default TRUE)}

\item{seed}{Random seed for reproducible sampling (default 42)}

\item{use_parquet}{If TRUE, also save parquet files alongside RDS (default TRUE)}
}
\value{
Path to the saved RDS file containing the SummarizedExperiment
}
\description{
Downloads RNA-seq gene expression data from the Genomic Data Commons (GDC)
for the specified project. Data is saved as a SummarizedExperiment RDS and
as parquet files (counts, sample metadata, gene metadata).
}
\examples{
\dontrun{
# Download 200 random samples with parquet output
rnaseq_file <- download_gdc_rnaseq(sample_limit = 200)

# Load the SummarizedExperiment
se_data <- readRDS(rnaseq_file)

# Or load parquet directly
counts <- arrow::read_parquet("data/raw/gdc/rnaseq_counts.parquet")
}
}
\seealso{
Other data-acquisition: 
\code{\link{acquire_commpass_data}()},
\code{\link{download_clinical_data}()},
\code{\link{download_s3_subset}()},
\code{\link{get_commpass_clinical}()},
\code{\link{list_s3_commpass}()},
\code{\link{query_commpass_rna}()}
}
\concept{data-acquisition}
