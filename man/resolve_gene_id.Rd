% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_gene_annotation.R
\name{resolve_gene_id}
\alias{resolve_gene_id}
\title{Resolve a gene symbol to the matching Ensembl ID in a SummarizedExperiment}
\usage{
resolve_gene_id(gene_symbol, se)
}
\arguments{
\item{gene_symbol}{Character. HGNC gene symbol (e.g., "CD70").}

\item{se}{A SummarizedExperiment, DESeqTransform, or matrix with Ensembl ID rownames.}
}
\value{
The matching Ensembl ID (character), or `NA_character_` if not found.
}
\description{
Checks if `gene_symbol` is already a rowname of `se`. If not, looks up the
symbol in `rowData(se)$gene_name` and returns the corresponding Ensembl ID.
}
\keyword{internal}
